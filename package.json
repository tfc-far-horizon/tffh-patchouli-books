{
  "name": "patchouli-markdown-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Server-backed collaborative editor for patchouli-markdown's WASM parser dialect",
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "convert": "node scripts/convert-book.mjs",
    "test": "node --test tests/*.mjs",
    "check": "node --check server.mjs && node --check lib/converter.mjs && node --check scripts/convert-book.mjs && node --check public/app.js && node --check public/merge.js && node --check public/ast-renderer.js"
  },
  "engines": {
    "node": ">=22"
  }
}
